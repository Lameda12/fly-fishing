__pycache__/
*.py[cod]
.DS_Store
node_modules/
web/dist/

# The upstream simulator is fetched, never vendored: embodied-fly-lab ships no
# LICENSE file, so its code and its ~91 MB connectome data are not
# redistributable here. tools/fetch_sim.py puts a local checkout in vendor/.
vendor/

# Derived from the fetched simulator, which ships no LICENSE, so neither of
# these is committed or redistributed here. Both are regenerated locally:
#   results/dn-cache*.json   python3 run.py cache
#   web/public/fly.glb       python3 tools/build_fly_glb.py
results/dn-cache*.json
web/public/fly.glb
