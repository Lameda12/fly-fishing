{
  "name": "fly-fishing-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Three.js viewer for fly-fishing recordings. Replay mode needs no backend.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "~5.9.0",
    "vite": "^7.1.0"
  }
}
